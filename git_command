git init                                  # Initializes the git repository 
git clone <http_git_url>                  # clones the remote repository/branch to local repository/branch
git branch                                # Provides list of all branch
git branch -r                             # Provides list of remote branch
git branch -a                             # Provides list of both remote and local branch 
git branch -v                             # Provide details with branch name
git branch <branch_name>                  # creates new branch
git checkout -b <branch_name>             # creates new branch and switches to that branch
git branch -d <branch_name>               # Deletes the branch locally
git push origin --delete <branch_name>    # Deletes the branch in remote
git branch -D <branch_name>               # Force deletes the branch
git branch -m <old_branch> <new_branch>   # Rename the branch
git branch --shor-current                 # Display current branch
git pull                                  # Pulls latest data from remote repo
git checkout <branch_name>                # Switches the branch
git config --global user.name <username>  # Configure git username locally
git config --global user.email <email>    # Configure git email locally
git status                                # Shows the changes file list
git add ./<filename>                      # Saves our changes . for all file changes and filename for specific file changes
git commit -m <commit_message>            # Freeze our changes
git push                                  # Pushes local changes to remote 
git merge <main/master>                   # combines the local changes in remote
git restore <file_name>                   # Reverts the changes like undo
git stash                                 # Stores changes temporarily
git stash pop                             # Restores the temporary stored changes like redo
git revert <commit_id>                    # Switches to specific commit and changes above the specific commit will be removed locally
git rm <file_name>                        # Deletes the file
git log -1                                # Shows the latest commit data
git log                                   # Displays commit history of that branch
git log --oneline                         # Displays modification list with commit id in one line
git diff <commit1> <commit2>              # Shows difference between two commits
git pull –rebase                          # Fetches changes from remote repository and rebases with current branch with updated branch 
git show                                  # Shows specific commit details including changes